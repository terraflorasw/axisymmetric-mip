# Orphaned pre-slug artefacts — quarantined 2026-08-25

These files have **no producing code and no doc provenance.** A slug must
reference a doc section (CONVENTIONS §7aw/§7ay); these can reference nothing.

    e1b  e1c  e1cc  sfprobe

`e1b` survives only as a **docstring example** in `journal.py`. No `.md` in this
programme defines any of the four; the only mention was the orphan table in
CONVENTIONS §7az, which is circular.

> **User, 2026-08-25:** *"if their provenance can't be derived from the docs,
> they really are orphaned. If we want them back, we have to re-derive them."*

That is `baselines.json`'s own rule applied to artefacts: *"nothing is inherited
from ../waveguide without re-derivation."*

🔴 **Do not cite anything in here.** It is retained only so a deletion is not
irreversible. If one of these turns out to matter, **re-derive it** under a slug.
