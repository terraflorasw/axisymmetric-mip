// GENERATED by cad/scadgen.py — DO NOT EDIT.
// Source: baselines.json + physics.design_point().
// Regenerate:  python3 cad/scadgen.py


// ── cavity ──
cav_r = 88.0045;   // mm  cavity radius (H1: D/L = 1.525)
cav_l = 115.416;   // mm  cavity length; z spans -cav_l/2 .. +cav_l/2
f0_ghz = 2.45;   // GHz  design frequency

// ── mode filter: corner groove, BOTH end caps ──
groove_w = 5;   // mm  radial width, measured inward from cav_r
groove_d = 10;   // mm  axial depth, OUTWARD past the end cap

// ── coupling loop ──
loop_d = 11;   // mm  depth the loop reaches into the cavity
loop_hw = 8;   // mm  HALF-width; area = loop_d * 2 * loop_hw
loop_rw = 1;   // mm  wire radius  ⚠️ TENTATIVE: never chosen
loop_gap = 0.3;   // mm  PORT gap at the crossbar  ⚠️ TENTATIVE
loop_phi = 36;   // deg  azimuth (sector centre)
cap_r_frac = 0.4805;   // 1   cap-mount radius as a fraction of cav_r

// ── series capacitor (barrel mount only) ──
gap2 = 0.5;   // mm  conductor break in the -y leg  [step-2 sweep]
flange_r = 1.9;   // mm  disc radius; 0.196 pF cancels 332 ohm  [swept]
flange_t = 0.3;   // mm  disc thickness (geometry.py default)

// derived, so the views cannot disagree with each other
loop_area = loop_d * 2 * loop_hw;   // mm^2
cap_r     = cav_r * cap_r_frac;     // cap-mount radius
barrel_xo = cav_r + 2;              // leg start, OUTSIDE the wall
barrel_xi = cav_r - loop_d;         // leg end, inside

