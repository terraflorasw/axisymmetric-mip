# DISCARDED — measured 2026-08-23 on a cavity with NO MODE FILTER

`GEO` never passed `--groove`, so every run here meshed `groove = [0.0, 0.0]`
while the design is frozen at **5 × 10 mm** (H2). These are design numbers for a
cavity nobody is building, and the results are all *about* the mode landscape —
which is exactly what a mode filter changes.

**Do not quote anything in here.** Kept only so the re-do can be compared against
what the wrong cavity said. See `CONVENTIONS.md` §7f, §7i, §7j.

⚠️ Also affected independently: η in these files is referenced to `Q_BARE =
44,384` (the **no-loop** value) while every driven mesh had a loop — the
with-loop reference is 29,854 (§7c).
